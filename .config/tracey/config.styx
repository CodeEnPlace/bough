specs (
    {
        name bough
        include ( **/*.md )
        impls (
            {
                name rust
                include (crates/**/*.rs)
                exclude (target/**)
            }
        )
    }
)
