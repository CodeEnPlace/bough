specs (
    {
        name bough
        include ( crates/**/*SPEC*.md )
        impls (
            {
                name rust
                include (crates/**/*.rs)
                exclude (target/**)
            }
        )
    }

)
