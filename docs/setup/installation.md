+++
title = "Installing Bough"
idx = 1
+++

While `bough` is still in alpha, the recommended way to install it is directly via `cargo`:

```bash
cargo install --locked --git https://github.com/CodeEnPlace/bough --bin bough
```
