[project]
name = "bough-example-pytest"
version = "0.1.0"
requires-python = ">=3.12"

[tool.pytest.ini_options]
addopts = ["--doctest-modules"]
testpaths = ["src"]

[dependency-groups]
dev = ["pytest>=8.0", "pytest-json-report>=1.5"]
